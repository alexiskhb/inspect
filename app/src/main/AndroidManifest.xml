<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dmitrii.testbtn" >
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Inspector"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
        <activity android:name=".MapActivity"
            android:label="@string/about_title">
        </activity>
        <activity android:name=".WhatSituation"
            android:label="Как обстановка в...">
        </activity>
        <activity android:name=".SituationIsBad"
            android:label="Беда случилась в...">
        </activity>
        <activity android:name=".AreaSituation"
            android:label="Обстановка в...">
        </activity>
    </application>

</manifest>